
1. **聊天窗层级更清晰**

   * 不需要显示会话事件
   * “思考 / 工具 Bash / 聊天”可以用不同图标或标签颜色区分。
   * **缩小信息密度**思考 / 工具 / MCP调用 较长，默认折叠，只显示关键信息
   * 工具调用块现在边框较重，建议弱化边框、增强标题，减少压迫感。

2. **输入区可以更精致**

   * 输入框和发送按钮高度略大，可以压缩一点。
   * 发送按钮颜色偏突兀，建议和整体黄/蓝主题统一。

3. **整体风格统一**

   * 现在聊天框偏工具软件风，桌宠偏可爱像素/二次元。
   * 可以把聊天框也做得更“软萌”：圆角更大、阴影更轻、浅色卡片、少一点硬边框。

4. AI弹窗窗口右上角有三个icon按钮（不使用文字），依次是
   * 对话历史，打开后弹出历史对话列表
   * 新对话，自动清空窗口内容，创建新对话
   * 关闭（现有的）

5. 右键桌宠退出时，同步隐藏状态到左侧的桌宠列表
6. AI弹窗的实现和“聊天”tab一致