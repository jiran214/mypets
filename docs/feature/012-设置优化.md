所有设置输入、修改内容都自动保存，删除”保存“”保存Agent设置“等按钮
桌宠首次显示在桌面时会出现在右下角的任务栏上
- 应用窗体大小和边框美化，增大窗口至常见应用大小，居中
- 通用设置
  - 删除”桌面显示“按钮
  - 增加桌宠名修改
  - 桌宠人设提供默认文本（400字左右），导入时该默认文本自动配置
  - 增加以下设置项
    - 置顶开关（默认关）
    - 重力开关（默认开，桌宠会自由落体到任务栏上）
    - “在资源管理器打开”按钮

皮肤设置改动
- 桌宠大小控制滑动条
- 自由调节开关（已有功能，桌宠右下角角标控制大小，开启则使用该功能）
- 当桌宠的状态为桌面显示，此时控制滑动条调节大小，应该有较长的延时


- 桌宠右键菜单
  - 样式美化
  - 所有动作变为二级选项，合为一级选项”动作”
  - ”设置“改为”主界面“，当点击主界面时，如果已在任务栏则会显示窗口
